services:
  - type: web
    name: careeropen-api
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "cd backend && gunicorn careeropen.wsgi:application"
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true

      - key: CORS_ALLOWED_ORIGINS
        value: "https://frontend-i6yrnbsn5-reuben-idans-projects.vercel.app"